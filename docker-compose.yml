version: '3.2'
services:
  proxy:
    image: 'apexapi/apex-proxy:1.0'
    ports:
      - '1989:1989'
    environment:
      - REDIS_HOST=config-store
      - REDIS_PORT=6379
      - PGHOST=logs-db
      - PGUSER=postgres
      - PGDATABASE=postgres
      - PGPASSWORD=password
      - PGPORT=5432
      - HTTPS_PORT=443
      - PORT=1989
    restart: always
    depends_on:
      - config-store
      - logs-db
  config-store:
    image: 'apexapi/apex-config-store:1.0'
    volumes:
      - '/data'
    restart: always
  logs-db:
    image: 'apexapi/apex-logs-db:1.0'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=postgres
    volumes:
      - '/var/lib/postgresql/data'
    restart: always
